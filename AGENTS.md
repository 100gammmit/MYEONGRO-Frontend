# MYEONGRO 작업 정책

이 문서는 저장소 내 기존 계획서, 에이전트 실행 문서, 작업별 지시보다 우선한다.
사용자가 대화에서 새 지침을 주면 해당 지침이 이 문서보다 우선한다.

## 작업 수행

- 작은 기능과 단일 작업은 주 에이전트가 직접 구현한다.
- 하위 에이전트는 서로 독립적인 작업이 2개 이상일 때만 사용한다.
- 파일이나 상태를 공유해 순차 실행이 필요한 작업은 하위 에이전트 수를 늘리는
  근거로 보지 않는다.
- 하위 에이전트를 사용하지 않아도 필요한 조사, 구현, 검증과 보고를 주 에이전트가
  끝까지 수행한다.
- 다른 작업자의 변경을 되돌리지 않으며, 공유 파일을 동시에 수정하는 하위
  에이전트를 배정하지 않는다.

## 하위 에이전트 배정

- 좁은 조사, 기계적 확인과 집중 리뷰에는 `gpt-5.4-mini`를 사용한다.
- 여러 파일 구현, 인증, 보안과 데이터베이스 작업에는 `gpt-5.4`를 사용한다.
- 최종 통합 리뷰가 별도로 필요할 때만 `gpt-5.5`를 사용한다.
- 하위 에이전트는 기본적으로 `fork_context: false`로 시작하고 필요한 작업
  맥락만 제공한다.
- 구현 작업에는 담당 파일, 완료 조건과 검증 명령을 명확히 지정한다.
- 다른 작업자의 변경을 되돌리지 말라는 지침을 포함한다.
- 최종 상태는 `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, `BLOCKED` 중
  하나로 보고하게 한다.

## 하위 에이전트 대기와 상태 확인

- 일반 구현 작업은 최소 15분, 읽기 전용 리뷰는 최소 8분이 지나기 전에는
  지연으로 판단하지 않는다.
- 짧은 `wait_agent` 호출을 반복하지 않고 충분히 긴 대기를 한 번 사용한다.
- 대기 시간 초과나 파일 변경이 없다는 사실만으로 교착 상태로 판단하지 않는다.
- 하위 에이전트 실행 중에는 겹치지 않는 조정이나 검증 작업만 수행한다.
- 최소 대기 시간이 지났거나, 에이전트가 `NEEDS_CONTEXT` 또는 `BLOCKED`를
  보고했거나, 외부 의존성이 실패했거나, 사용자가 즉시 상태 확인을 요청했을 때만
  비중단 상태 확인을 보낸다.
- 사용자가 작업을 변경·취소했거나 에이전트가 명백히 잘못되거나 파괴적인 작업을
  수행하는 경우가 아니면 실행을 중단하지 않는다.

## 교착 판단

다음 조건을 모두 만족할 때만 하위 에이전트가 교착 상태라고 판단한다.

1. 최소 대기 시간이 지났다.
2. 비중단 상태 확인 후 5분 동안 응답이 없거나 에이전트가 `BLOCKED`를 보고했다.
3. 진행 중인 명령이나 외부 작업이 지연을 합리적으로 설명하지 못한다.

교착 상태가 확인된 경우에만 작업 범위를 줄이거나 다시 배정한다. 기존 변경은
보존하고 원인은 마일스톤 Notion 보고에 기록한다.

## 리뷰

- 구현 에이전트마다 별도의 명세 리뷰나 코드 품질 리뷰 에이전트를 자동으로
  배정하지 않는다.
- 필요한 리뷰는 주 에이전트가 변경 범위와 위험도에 맞춰 직접 수행한다.
- 사용자가 요청했거나 마일스톤 수준의 독립 리뷰가 실질적으로 필요할 때만 별도
  리뷰 에이전트를 사용한다.

## 테스트와 검증

- 각 구현 단계에서는 변경과 직접 관련된 테스트를 실행한다.
- 전체 테스트, 타입 검사, 린트, 빌드 같은 종합 검증은 마일스톤 완료 시 마지막에
  한 번 실행한다.
- 고위험 변경이나 사용자의 명시적 요청이 있으면 더 넓은 검증을 앞당겨 실행할 수
  있다.

## Notion 보고

- 보고 작성은 '오더/보고 로그' 데이터 베이스에 작성한다.
- 하위 에이전트 사용 여부와 관계없이 마일스톤이 완료되면 Notion 작업 보고를
  작성한다.
- 개별 작업이나 하위 에이전트마다 페이지를 만들지 않고, 완료된 마일스톤 전체를
  요약한 페이지 1개를 작성한다.
- 페이지에는 목표, 주요 변경, 검증 결과, 미해결 사항과 다음 마일스톤을 기록한다.
- 도구 호출의 시간 초과는 실패 결과로 기록하지 않는다.
